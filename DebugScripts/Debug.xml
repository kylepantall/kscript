<!-- 
    KScript treats any node without child elements as a single node. 
    This means a "dowhile" without any elements isn't handled.
    In order for any KScript Object node containers to handle nodes they must 
    contain elements so the parser can treat them as a collection of nodes 
    and pass them to the appropriate Operator handler.
-->

<kscript dynamic_defs="yes" throw_exceptions="yes" quiet="no" print_info="no">
    <!-- <marray id="data" _insertion="" _retrieval="@from_base64()" />

    <closure in="">

    </closure>

    <callback name="handler" in="input">
        <return>@to_base64($input)</return>
    </callback>

    <def id="item">@to_base64('HELLO MY NAME IS $value')</def>
    <echo>$item</echo>

    <echo>@from_base64($item)</echo>

    <minsert at="~data">
        <item>@to_base64('Welcome')</item>
    </minsert> -->

    <!-- 
    <for to="i" while="$i->zero()" math="$i->increment()">
        <echo trail_newline="no">$i</echo>
    </for> -->


    <def id="values">@tie('Hello','Maybe','10','20')</def>


    <!-- <echo>$values->1()</echo>
    <echo>$values->2()</echo>
    <echo>$values->3()</echo>
    <echo>$values->4()</echo> -->

    <echo>$values</echo>
</kscript>